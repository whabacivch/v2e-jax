# Minimal portable runtime.
# Install the backend-specific JAX wheel that matches your Linux target:
#   CPU:          pip install -U jax
#   NVIDIA GPU:   pip install -U "jax[cuda13]"
#   AMD ROCm GPU: pip install -U "jax[rocm7-local]"
jax>=0.4.30
numpy>=1.26
pillow>=10.0
